- name: (zsh) install packages
  tags:
    - zsh
  community.general.pacman:
    name: "{{ item }}"
    state: latest
  loop:
    - zsh
    - zsh-syntax-highlighting
    - zsh-completions
    - zsh-autosuggestions
