#!/bin/bash

# Check if Emacs daemon is running
if pgrep -x "emacs" > /dev/null
then
    # If Emacs is running, open a new client
    emacsclient -nc "$@"
else
    # If Emacs is not running, start the daemon and open a client
    emacs --daemon
    emacsclient -nc
fi
