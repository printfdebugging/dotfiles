#!/usr/bin/env bash


targets=(
    $HOME/.thunderbird
    $HOME/.gitconfig
    $HOME/.pubkeys
    $HOME/.privkeys
    $HOME/.mozilla
    $HOME/.cache
    $HOME/.gnupg
    $HOME/.ssh

    $HOME/downloads
    $HOME/repos
)


for target in ${targets[@]}; do
    rm -rf $target
done


