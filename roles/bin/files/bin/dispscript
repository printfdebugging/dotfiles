#!/bin/bash

isLaptop=$(xrandr -q | grep "eDP" | wc -l)
wallpaper="$HOME/.local/share/wallpapers/greens.jpg"

if [ $isLaptop -ne 0 ]; then
    xrandr --output eDP --mode 1920x1080 
    xrandr --output HDMI-1-1 --mode 1920x1080 --primary --left-of eDP
fi

xwallpaper --zoom $wallpaper
