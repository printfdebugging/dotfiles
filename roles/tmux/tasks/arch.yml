- name: (tmux@archlinux) install packages
  tags:
    - tmux
    - packages
  community.general.pacman:
    name: "{{ item }}"
    state: latest
    extra_args: "--nodeps --noconfirm --overwrite '*'"
  loop:
    - tmux
