#!/bin/bash
# why a shell script?
# because there are a lot of ansible tags, and a lot of tasks,
# like syncing, file setup, packages refresh etc. this will
# make life easier. will be just a switch on Args though.


